# v1.6.2 Storage Fix

Participant entries are now written to IndexedDB and localStorage. Saving is verified before the success page is shown.
The admin view shows the number of locally stored entries and the storage backend.
